# 2026-08-28

## 内容自动投喂地基（三梵独立站 GEO 延伸）
- 新增 `rss.xml`（6 篇博客 item，RFC822 pubDate，description 为摘要级）。
- 新增 `llms.txt`（AI 爬虫索引说明书：品牌定位 + 核心页 + 6 篇博客 URL + 何时引用指令）。
- 首页 / 博客页 `<head>` 加 `rel="alternate" application/rss+xml` 订阅链接。
- `robots.txt` 加 `LLMS-TXT:` 指令；`sitemap.xml` 加 `rss.xml` 条目。
- 输出 `三梵内容自动分发方案.md`：RSS 投喂原理 + Dev.to/Hashnode RSS ingestion（零成本档1）+ Zapier/Make 全文 API 分发（档2/3）+ canonical 模板 + 执行 CheckList。
- 部署 commit `4dabb13`；Playwright 验证线上 rss.xml/llms.txt/robots.txt 均 HTTP 200、6 item、RSS link 存在。
- 关键坑：外部平台同步必须带 canonical 回独立站，否则被 Medium/Dev.to 抢排名；Dev.to RSS ingestion 默认摘要级，要全文需 RSS 加 `<content:encoded>`（待办，用户未要求）。
- 后续：用户侧需在 Dev.to/Hashnode 填 RSS URL 才能真正自动流；Zapier/Make 全文分发需各平台账号 Token。

## GEO 每周 AI 曝光自检清单
- 新增 `GEO每周AI曝光自检清单.md`：照 GEO 方案第四步落地的可勾选清单。
- 结构：基本信息 + 准备(ChatGPT/Gemini/Perplexity 三入口) + 问法(像采购商问品类词) + 5 组监测词表 + 15 格检测记录表(5词×3 AI) + 判定标准(第3-5周起引用/第8周来信) + ✗ 补强动作 + 每周收尾。
- 5 组词与 GEO-Content-Plan.md 完全一致：best yoga mat manufacturer china / custom TPE yoga mat OEM / low MOQ yoga mat factory / private label yoga mats supplier / Fujian yoga mat factory。
- 部署 commit `ce032e1`。

## RSS 升级为全文版（content:encoded）
- 背景：Dev.to/Hashnode 的 RSS ingestion 只发摘要（原 description 摘要级）；用户已填好 Dev.to RSS，故升级全文。
- 做法：临时脚本 build-rss.js（已删）抽取每篇博客 `<div class="lang-en">` 块（div 平衡计数，避免正则误判嵌套）→ 剔除 script → 相对路径转绝对 URL → 顶部加原文回流声明 → 生成 `<content:encoded>` CDATA；RSS 头加 `xmlns:content`。
- 验证：XML 合法、6 items、全文 1800–2500 字符、每篇 1 个绝对图片链接、0 相对路径残留。
- 部署 commit `bc28bcd`（顺带把代理失败未推的 6e05b6f Dev.to 路径修正一起推上）；线上验证 content:encoded 6 块、绝对链接 6 个。
- 关键坑：Playwright `ctx.request.get` 走 Node 端网络（沙箱无外网）必然超时；必须在 `page.evaluate` 里用浏览器端 fetch（先 goto 同源页再 fetch）才能访问线上资源。
- 待办：用户在 Dev.to Extensions 页需手动点 Fetch feed 重抓（已抓的摘要草稿不会自动更新，已发布的需手动改或删后重发）。

## 海外推广渠道增补 + 平台营销技能安装
- 新查到 7 个高意图渠道，增补进 `三梵海外推广渠道地图.md`：垂直 B2B 社区(Slack/Discord/行业论坛)、UTM+GA4 追踪、微网红/瑜伽馆主合作、YouTube Shorts、视频嵌入产品页、播客嘉宾、Newsletter 赞助/Retargeting。部署 commit `ae601f6`（代理失败已补推）。
- 用户同意安装两个 WorkBuddy 营销技能，按新技能强制流程先审计（skills-security-check 平台未提供，改手动审计）：
  - `marketing-skills`（v1.0.0，display 营销技能库）：纯 Markdown 参考库 23 模块，无脚本无执行 → **P2 安全**。
  - `seo-ops`（v1.0.2）：4 个 Python 脚本（gsc_client/gsc_auth/content_attack_brief/trend_scout），网络终点全为合法服务（Google OAuth 只读作用域、Ahrefs、Google Trends、HN、Reddit、Brave），无 rm -rf/os.system/subprocess/eval/凭证外泄，GSC token 仅存本地 .gsc-token.json → **P2 安全**。SKILL.md preamble 引用不存在的 telemetry/ 目录（静默失败，无害）；README 含推广链接（非代码）。
  - 结论：两个均无 P0/P1 风险，已安装到 ~/.workbuddy/skills/，可正常使用（seo-ops 需用户配 GSC OAuth + 可选 Ahrefs/Brave key 才生效）。
- 可复用：安装外部技能时若平台无 skills-security-check，改手动审计——Grep 风险模式（rm -rf/os.system/subprocess/eval/base64/curl 未知域/读 ~/.ssh//etc/）+ 列出所有 http(s) 终点确认无外泄；安装≠启用，装后读 SKILL.md 与脚本扫描即可，不执行其中命令。

## SEO 站内体检（marketing-skills/seo-audit 模块）
- 用自写脚本 `seo_audit.py`（标准库 HTMLParser，无需依赖）对全站 13 页逐页抓取 title/desc/canonical/h1/h2/h3/图片alt/内链/JSON-LD/OG/lang，输出信号表 + Issue Scan。
- 综合评分 B-（地基扎实）。核心问题：①全站缺 canonical（P1）；②product.html 是 JS 渲染模板，静态仅 107 词、title 统一 "Product — SANFAN"，被 products.html 17 个产品卡链接（P1，建议静态化或按 id 动态 title/canonical）；③缺 OG/Twitter Card 社交标签（P2）；④首页/产品/联系/博客列表页缺结构化数据（P2，FAQ+博客已做）；⑤部分 title(faq/vet/7day/custom) 与 desc(index/products/faq) 超长被截断（P2 快赢）；⑥博客双语块双 H1（P3）；⑦缺 hreflang（P3）；⑧product.html 1 图缺 alt（P3）。
- 已做对：图片 alt 99%、FAQ+博客 JSON-LD 就位、robots 引 sitemap+llms.txt、内链健康 16-44、核心页词数足。
- 输出 `三梵独立站-SEO站内体检报告.md`（评分卡+逐页表+问题明细+行动清单）。
- 下一步：站内维度已完；要查「能否搜到/排名」需用 seo-ops 跑 GSC 体检（需用户给 GSC OAuth）。可先动手低风险快赢：canonical+OG+描述修剪（P1/P2 低工作量）。

## SEO 快赢已落地（用户确认"把能做的先做了"）
- 用脚本 `seo_fixes.py`（幂等）对全站 13 页：①加 self-referencing `<link rel=canonical>` 绝对 URL；②加 Open Graph(og:type/title/description/image/url)+Twitter Card(summary_large_image)，og:image 按页配代表图(assets/img/*.jpg)，& 转义为 &amp; 保证合法；③精确修剪超长 title(4 篇博客+faq)与 desc(index/products/faq) 到 SERP 友好长度。
- 用脚本 `seo_structured.py` 补结构化数据：首页 Organization+WebSite @graph、products.html ItemList(8 产品)、contact.html LocalBusiness(地址/电话/邮箱)。
- `product.js` 的 render() 增加按 `?id=` 动态写 canonical/og:title/og:description/og:image/og:url/twitter（每个产品详情页独立标注）。
- 验证：13 页 canonical/og/twitter 全 True、JSON-LD 全部合法、OG 无裸 &；product.js `node --check` 通过。issue scan 中 canonical 缺失与超长 title/desc 已清零，仅剩「无结构化数据」(已补3页)、product.html 薄/JS(P1 大改待定)、博客双语双 H1(P3)。
- 提交 commit `4b607ea`，已 push origin/main（PAT+HTTP/1.1 注入，ae601f6..4b607ea），Vercel 自动部署。
- 遗留待确认：product.html 静态化/预渲染（P1，工作量大，单独确认）、博客双 H1 收口(P3)、hreflang(P3)。

## 竞品调研 + product.html 静态化（用户确认"先找对标、再同步静态化"）
- 竞品对标 8 个专业工厂站（hunlon/ttsports-group/yogafitness.group/archiyoga/bogasports/parnalsports/theyogamats/made-in-china 工厂店）。共性：干净静态 URL（非 ?id=）、每产品独立富内容页 800–2000+ 词、规格表+FAQ+认证+订单流程+Why choose us、Product Schema。
- 发现既有 bug：`products.html` 的 ItemList JSON-LD 里 4 个 URL 用了不存在的 id（roller1/bands1/ball1/db1），点进 product.html 是"未找到"。静态化时一并修掉（映射为 fr1/rb1/rb2/fi5）。
- 静态化做法：`gen_product_pages.js`（Node，读 products-data.js + 从 main.js 抽 I18N 对象 brace-match 提取）生成 `products/<id>.html` 共 27 页（ym1-8/pr1-5/fr1-5/rb1-4/fi1-5）。每页：独立 title/desc/canonical(绝对)、OG+Twitter Card、Product+BreadcrumbList JSON-LD、EN 内容静态渲染 + ZH 用 `.lang-en/.lang-zh` 双块切换（沿用站点机制，main.js 仅填 nav/footer/CTA 标签、不加载 product.js）；相关产品卡直接作为 `.prod-grid` 子元素避免 grid 布局破坏；内联脚本做缩略图切换 + 中英文 WhatsApp 链接同步。
- 配套：`products.html` 全部 27 个产品卡链接改 `products/<id>.html`（含修复 4 幽灵 id）；`product.js` 顶部加对已知 id 的 `location.replace('products/<id>.html')` 重定向（consolidate 旧 ?id= 权重，未知 id 仍 notFound）；`sitemap.xml` 加 27 产品页。
- 验证：27 页 JSON-LD 合法、canonical/OG/双块/相对路径 ../ 全 OK、标签平衡、product.js `node --check` 通过、ym1 静态 EN 词数 ~780（原 JS 版 107）、无 JS 渲染占位符。
- 提交 commit `12ed990`（领先 origin/main 1 个）。推送先因 WorkBuddy 透明代理（127.0.0.1:49513）CONN 失败受阻；22:48 代理自恢复后第 1 次重试即 `git push origin main` 成功（`4b607ea..12ed990`），Vercel 已自动触发部署。
- 经验：本执行环境 GitHub 访问走 WorkBuddy 注入代理（HTTP_PROXY=127.0.0.1:49513），代理偶发 CONN/Empty reply/502 故障，纯属平台侧、非代码；遇到时静默重试即可，代码本地提交始终安全。

## SEO P3 收尾（双 H1 + hreflang，用户确认"两个一起做"）
- 双 H1 修复：`blog.html` 列表页 + 6 篇博客文章页的 ZH 块主标题原为 `<h1>`（与 EN 块各一个，违反单 H1 最佳实践）。脚本 `fix_dual_h1.py` 把每页最后那个 h1（ZH 块）降级为 `<h2 class="post-title">`（列表页 ZH h1 带内联 style，文章页无属性）；CSS 加 `.article .lang-zh h2.post-title` 复用 `.article h1` 字号，视觉不变。验证 7 页 H1 全变为 1。
- hreflang：脚本 `add_hreflang.py` 基于 canonical 绝对 URL，给全站 40 页（7 核心 + 6 博客 + 27 产品）注入 `en` + `zh` + `x-default` 三行自引用（本站双语同页单 URL，按 Google「单 URL 多语言」规范）。幂等（已有则跳过）。
- 验证：40 页 H1=1、hreflang 三行齐全、JSON-LD 合法、标签平衡，零回归。生成器 `gen_product_pages.js` 模板同步加 hreflang，保证未来重跑产物一致。
- 提交 commit `641b631`，push 成功（`12ed990..641b631`），Vercel 自动部署。至此 SEO 站内体检报告列出的问题除「产品页静态化(已做)、结构化数据(已做)」外全部闭环。
- 遗留（非体检项，本次发现未动）：6 篇博客 ZH 块内混有英文 CTA 小标题 `<h3>Planning a private-label launch?</h3>` 等，属双语内容不一致，可后续顺手中文化。
