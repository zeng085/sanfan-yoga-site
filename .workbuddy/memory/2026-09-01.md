2026-09-01 00:48 修复 Google Search Console 产品结构化数据报错：将 AggregateOffer 的 lowPrice/highPrice 改为数字，移除 availability/priceValidUntil；审计并修复 189 个产品页；提交 ad04a21 并 push；创建 skill fjsanfan-product-schema。
00:58 修复产品页「规格总览」卡片排版：7 个语言版 products.html 里的字面量 \\n 换成真 <br>（语言页 applyLang 早退、JS 不兜底，之前直接把 \\n 当文字印出来且挤成一行）；清掉 spec1 重复的 (5/32"–1/2")；移动端 ≤640px 规格栅格改单列 + 卡片内边距收紧 + overflow-wrap；bump ?v=20260901（288 个 HTML）。commit b21aaa2 已 push，线上已验证。
01:09 修复页脚 logo 变形：`.site-footer .brand img` 只设了 height:34px，没设 width:auto，导致在某些浏览器里被拉成宽短矩形；补上 `width:auto; object-fit:contain;` 保持 500×500 比例；bump ?v=20260901a（288 个 HTML）。commit 11b736d 已 push，线上已验证。
01:27 审阅《fjsanfan_URL_规范化专项整改报告》：核实后确认报告 P0（FR/IT/ES 缺 index.html 重定向）成立，已补 3 条 + 额外补 6 条语言目录无斜杠重定向（/fr → /fr/），commit 07029d9 已 push 并线上验证 11 条全部 308。报告 3.4「zh 指向英文根域」与事实不符（是同页 button[data-setlang=zh]，非链接），且删中文入口与既定策略冲突，不采纳。另发现报告漏项：www.fjsanfan.com 未 301 到主域（当前 502）、product.html 为孤儿页（不在 sitemap、无内链）。hreflang 全站 287 页集合一致、自引用与回链 100% 通过；sitemap 287 条无 index.html，抽样 50 条全 200。
01:40 核实 fjsanfan.com 的 DNS/部署架构：NS 在 Cloudflare（lana/noah），但主域 A 记录是灰云（216.198.79.1/64.29.17.1 → Vercel），响应头无 cf-ray，流量不经 CF；www.fjsanfan.com 在 CF 里无 DNS 记录（NXDOMAIN），此前测到的 502 是本地代理伪造。结论：CF 重定向规则需记录开橙云才生效，主域保持灰云避免与 vercel.json 冲突。已写入 MEMORY.md 第 13 节 + 待办。
09:30 修 about 页 "Factory at a Glance" 4 张统计卡高低不齐：CSS .stat-card .num 加 white-space:nowrap + line-height:1.15（㎡ 上标不再撑高 line-box），.stat-card 改 justify-content:flex-start 让 num 顶部对齐；7 个语言版 about.html 拆 stat4 数字+单位（DE/FR/IT/ES 的"12 Lines/Zeilen/lignes/..."改成 num="12" + lab=独立文案；EN/ZH 的 main.js 同步；JA/KO 不动，CJK 不换行）；bump ?v=20260901b。commit 12151d8 已 push，7 语言线上验证 stat4 全部正确。
09:36 修首页 Factory at a Glance 第 4 张卡被右图压住：CSS 在 ≤1100px 把 .stat-grid 改成 2 列（之前只在 ≤980px 才改，980-1100px 区间 4 列挤在 ~115px 列宽里）+ .split gap 收紧到 28px；首页 7 个语言版 stat4 HTML 同步拆 num=数字 / lab=单位（5 Lines / 5 Zeilen / 5 lignes / 5 righe / 5 líneas / 5줄 全部拆好；JA 5行 不动，CJK 不换行）；全站 ?v=20260901b→20260901c。commit 888b8da 已 push，线上 CSS 新断点和 EN/DE/FR/JA stat4 HTML 均已验证。
14:30 修中文/英文/语言页页头移动端排版（用户截图反馈"中英文的页头排版也有问题"）：根因是 69 个 HTML 的 `.nav-actions` 在 `<a class="btn btn-primary">` 之前多写了一个 `</div>`，导致 Get a Quote/获取报价/Angebot 实际是 `.container.nav` 直接子级而非 `.nav-actions` 子级 → `.menu-toggle` 也掉到 `<header>` 直接子级，CSS `.nav-actions .btn{display:none}` 永不命中，移动端按钮和 logo+语言切换挤成一团。修复：写 /tmp/fix_nav_structure.py 4 种 markup 变体（变体 A：EN/ZH + products/ 共 33 个；变体 B_v1：DE/JA/KO/FR/IT/ES 主页面 30 个；变体 B_v2：6 个 blog.html，`</div></div>` 后是空行 `\n\n` 而非 `\n</div>`）正则批量修复，70 个文件命中 69 个（孤儿页 product.html 无害未修）；副带：6 个语言站 blog.html 的报价按钮链接 `../contact.html` → `contact.html`（之前会跳英文联系页）。CSS 新增 @media (max-width: 920px) nav 专属断点（iPad portrait 768px 6 个链接+语言+按钮会挤爆，比 980px 内容栅格更激进）。修复后我又引入一个瑕疵——把 `<button class="menu-toggle"class="menu-toggle">` 重复 class 写出来（63 个文件），浏览器忽略重复属性但属无效 HTML，写 /tmp/fix_dup_menu_toggle_class.py 幂等清理 69 个文件（复查 0 残留）。bump `?v=20260901e`（288 个 HTML）。commit 18ae8dd（合并 14ca49d amend）已通过 /tmp/push_via_api2.py（GitHub REST API + tree API inline content + 502 指数退避重试，289 个文件从 6 分钟降到十几秒）推到 GitHub 触发 Vercel 部署，git fetch --depth=1 拉回对齐本地，curl 线上 styles.css?v=v20260901e 与 menu-toggle 标签已生效；Playwright 在 EN/ZH/DE 三档视口（390 / 768 / 1280）验证 btn_in_nav_actions=True / mt_in_container_nav=True，移动端按钮 display:none、桌面 display:flex，符合预期。\n