# 2026-08-29

- 站点域名从 `sanfan-yoga-site.vercel.app` 切换为 `fjsanfan.com`（Cloudflare 代理 → Vercel）。全站 53 个文件做域名字串替换：canonical/OG/hreflang/JSON-LD/sitemap.xml/rss.xml/llms.txt + 40 个 HTML + 生成器脚本(gen_product_pages.js/seo_fixes.py/seo_structured.py) + 6 个运营 Markdown 文档。
- 新增 `vercel.json`：把旧 `sanfan-yoga-site.vercel.app/:path*` 永久(308)重定向到 `https://fjsanfan.com/:path*`，保 SEO 权重。
- 校验：40 页 canonical/og:url/hreflang 全用新域名、JSON-LD 合法无旧域残留、sitemap/rss XML 格式正确、vercel.json 合法、robots.txt Sitemap 指向新域名。
- 提交 `fe7aa4f` 已 push 成功，Vercel 自动部署。历史 `.workbuddy/memory/*.md` 里的旧域名叙述未动（属历史记录）。
- 注意：Vercel 的 `permanent:true` 在 vercel.json redirect 中返回 308（等同 301 传权重），无需改。CF 端若需旧域跳新域也可在 CF 规则补一条，但 vercel.json 已覆盖。
