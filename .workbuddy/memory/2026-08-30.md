# 2026-08-30

## T1 多语言站点（DE/JA/KO）— 分语言 URL 骨架落地

**目标**：按品类真实卖得好的区域配语言（德国=欧洲最大单一市场、日韩=非母语不转化），T1 先做 DE/JA/KO。
站点原为「单 URL 内 EN+ZH 双块 JS 切换」，排不进本地语搜索，故改为**分语言独立 URL**。

**已上线（推送 `39be1aa` + 修复 `eca1167`）**
- `/de/` 全站 10 页、`/ja/` 3 页（index/about/products）；静态单语页，独立 `html lang`/canonical/OG/`Product+BreadcrumbList` JSON-LD
- hreflang 六向互链（en/zh/de/ja/ko/x-default），**只指向真实存在的页面**（避免指向 404 这种 SEO 错误）
- 导航加多语言切换器（EN/中文/DE/JA/KO），中文走 `data-setlang` 写 localStorage 后跳英文页同页切换（main.js 新增该逻辑）
- sitemap 增补语言页并写 `xhtml:link` hreflang
- 验证：德语 DIM-2 术语/结构异常 0.4%、DIM-3 语言/长度 0.3%；日语 1.0% / 0.5%；已上线页未译率 1.6–1.8%

## 翻译引擎与验证工具链（i18n_build/，可复用）
- `translator.py`：MyMemory 批量引擎。批量拼接（480B/块，`\n` 分隔保留换行）+ 术语保护 + 行业术语表 + 并发6 + 磁盘缓存断点续翻
- `verify.py`：四维验证 —— DIM-1 回译语义一致性 / DIM-2 术语锁定+结构完整性 / DIM-3 语言正确性+长度比 / DIM-4 批量vs单条交叉
- `build_pages.py`：文本节点级翻译 HTML，保留全部标签与 SEO 结构；剥离 lang-zh 块、移除 data-i18n（防止 JS 语言切换覆盖译文）
- `fix_cache.py`：**零 API** 修复缓存里的占位符残留（protect 是纯函数，可由源文本重建映射重放 restore）
- `do_continue.py`：续翻调度器（逐页子进程 + 未译率门禁 + 自动推送）
- `update_seo.py`：hreflang 互链 + sitemap 同步

## 踩坑记录（重要）
1. **占位符必须用纯 ASCII**：非 ASCII 的 `☁TM0☁` 会被翻译引擎改写/吞字符导致泄漏。用 `ZXQTM0QXZ`。
2. **占位符会被插入空格**：日语返回 `ZXQTM 0 QXZ`，精确 replace 失败 → restore 需多档宽松正则 + 残片兜底清理。
3. **PROTECT 正则必须加词边界**：`PU` 会误匹配 "out**pu**t"、`NR` 会误匹配 "Faszie**nr**ollen"。
4. **数值+单位不能加 `\b`**：`㎡` 非 `\w`，`\b㎡\b` 匹配失败 → `12,000㎡` 被误译成 `12.000 €`（面积变欧元）。改用 `\d[\d,.\s]*(?:㎡|m²|sqm)`。
5. **未译率统计口径**：短标签（FAQ/Blog/SANFAN/2013/100 pcs/ISO 9001/邮箱电话/品牌）保留原文是**正确行为**，计入分子会把合格页面误判不合格。只统计长度 ≥20 字符且非专有名词的条目。
6. **续翻脚本绝不能重判已存在页面**：曾误把已上线的合格页面（未译率误报 8.2%）移入 `_pending`，破坏已交付成果。改为「已存在一律 skip，只补缺失页」。
7. **zsh 不做变量自动分词**：`for P in $PAGES` 在 zsh 下当成一个整体，必须用数组 `PAGES=(a b c)`。
8. **长驻进程会被 OOM kill（exit 137）**：翻译/生成多页时必须逐页独立子进程。
9. **MyMemory 对 429 要快速失败**：否则重试 4 次×递增 sleep，整批处理卡死十几分钟。

## 后续：用户提供 DeepL key，T1 全部完成（推送 `f15110b`）

用户提供 DeepL Free key（`:fx` 后缀 → 必须用 `api-free.deepl.com` 域名，额度 100 万字符/月）。
**已用 DeepL 把 de/ja/ko 三语 30 页全部重翻**并替换原 MyMemory 译文。

### DeepL 集成关键（与 MyMemory 方案的最大差异）
- **术语保护必须用 `tag_handling=xml` + `ignore_tags=["x0","x1",...]`，不能用字符串占位符**。
  实测：占位符 `ZXQTM0QXZ` 会被保留但**干扰引擎理解句子**（日语输出语义变形）；
  换成 `<x0>TPE</x0>` 标签后，标签精确保留且译文自然。
  行业术语表同理：`<x1>Faszienrollen</x1>` 直接写入目标语译法。
- **保护范围要"够用即可"**：锁太多会让引擎看不懂结构而误译。
  实测教训：锁了 MOQ/OEM/ODM/pcs 后，德语把 "MOQ from 100 pcs, 7-day sampling, REACH..." 译成
  "konform mit den Normen „...“"（把列表当标准名）。
  改为**只锁品牌+认证+材料+尺寸单位**后，DeepL 自行处理得更专业：
  `Mindestbestellmenge ab 100 Stück` / `最小注文数量（MOQ）100枚` / `최소 주문 수량(MOQ) 100개`。
- **回译必须显式传 source_lang**：曾硬编码 `source_lang="EN"`，导致 `back_translate` 把德语当英语翻成德语
  （后又因 `lang==src` 直接 return 原文），DIM-1 完全失效。已加 `src` 参数贯穿 `translate_lines/_call_batch/_ck`。

### 最终四维验证结果（936 条/语言，全量或抽样）
| 语言 | DIM-1 回译(抽样110) | DIM-2 术语/结构(全量) | DIM-3 语言/长度(全量) | DIM-4 交叉(抽样25) |
|---|---|---|---|---|
| DE | 0.749，异常 1.8% | 0.1% | 0.1% | 1.000，0% |
| JA | 0.765，异常 0% | 0.0% | 0.4% | 1.000，0% |
| KO | 0.780，异常 0% | 0.4% | 0.3% | 1.000，0% |

### DIM-1 评分算法的坑（重要）
回译**天然会改写措辞**（`Wide format for 2 persons` → `Wide size for two people`），
词面 Jaccard 会被同义改写压到 0.167，误判为失败。
修正：改为 `0.7×字符级模糊匹配 + 0.3×Jaccard`，并把 DIM-1 明确定位为
**「严重跑偏粗筛」**（精确判定交给 DIM-2/3/4）。修正后平均相似度 0.75–0.78，符合预期。
另：回译接口偶发返回空串（多为极短文本），需跳过判定而非计为失败。

### 验证规则去噪清单（都是"翻译正确但被误判"）
- 数量词缀本地化：`Tri-fold`→`3つ折り`/`3단`、`double-layer`→`2층`、`Third-party`→`제3자`
- 数字单词互转：`5 lines`→`fünf Linien`、`within 1 business day`→`innerhalb eines Werktages`
- 数量级换算：`200K+`→`20万人以上`；习语 `24/7`→`24時間年中無休`
- 纯数字文本（`200K+`→`200.000+`，德语用点作千分位）不做字符集判定
- 地址/公司名/规格串/品牌/邮箱电话 保留原文属正确，不计未翻译

### 状态
- 30 页全部上线，sitemap 收录 30 条语言页，hreflang 六向互链齐全
- DeepL 用量约 3.5 万字符 / 100 万（3.5%）
- 自动化：一次性补翻任务已删除；每日续翻任务改为 PAUSED 并改写 prompt 为「T2 扩展指南」（含需改动的文件清单）
- T2 候选：FR / IT / ES（西欧主力），T3：PL / NL / PT

---

## 严重故障：Vercel 部署静默卡死（域名切换后所有改动都没上线）

**现象**：线上 `sitemap.xml` 仍用旧域名 `sanfan-yoga-site.vercel.app`、语言页 0 条；
`/de/`、`/ko/index.html` 全部 404。但 git push 都显示成功。

**根因**：`vercel.json` 的 redirect 写成了
```json
{ "source": "https://sanfan-yoga-site.vercel.app/:path*", "destination": "https://fjsanfan.com/:path*" }
```
Vercel 的 `source` **必须是路径模式**，写成完整 URL 会导致构建配置错误 → 部署失败。
该 vercel.json 是域名切换（fe7aa4f）时引入的，也就是说**从那之后的改动一直没上线**
（SEO 修复、静态产品页、hreflang、多语言……全都卡住）。

**正确写法**（跨域名跳转要用 `has` 匹配 host）：
```json
{
  "redirects": [{
    "source": "/:path*",
    "has": [{ "type": "host", "value": "sanfan-yoga-site.vercel.app" }],
    "destination": "https://fjsanfan.com/:path*",
    "permanent": true
  }]
}
```
修复后推送 `3ea92d6`，部署立刻恢复：sitemap 用新域名、`/de/` 200、DE/JA/KO 各 10 页全部 200。

**教训**：git push 成功 ≠ 部署成功。以后做完站点改动，必须 curl 线上 URL 验证实际内容
（尤其检查 sitemap 里的域名/条数是否符合预期），不能只看 push 输出。

## 收录提交（IndexNow 已配置完成）

- 生成 IndexNow key `92c62d163567679a15cfefe662a3eb43`，key 文件部署在站点根目录
  `https://fjsanfan.com/92c62d163567679a15cfefe662a3eb43.txt`（**不能删**，IndexNow 会校验）
- 脚本 `i18n_build/submit_indexnow.py`：从线上 sitemap 取 URL，推送到
  api.indexnow.org / bing.com / seznam.cz / yandex.com
- 首次推送结果：api 202、bing 200、seznam 200、yandex 202，共 70 条 URL（DE/JA/KO 各 10）
- **Bing 特别重要**：ChatGPT 搜索使用 Bing 索引，是 GEO 的直接入口
- Google 不支持 IndexNow，必须手动：GSC 添加网址前缀 → Cloudflare 加 TXT 记录验证
  （`google-site-verification=...`，名称 @）→ 提交 sitemap → URL 检查工具手动请求索引
- 指南文档：`三梵站点收录提交指南.md`

## 用户报的两个问题及修复

### 问题1：英文/中文页看不到其他语言
**原因**：语言切换器（`.lang-switch`）只在生成 `/de/ /ja/ /ko/` 页面时写入（build_pages.py 替换
`<button class="lang-btn">`），**英文原页从未加过**，所以 `/` 下的页面只有孤零零的「中文」按钮。

**修复**：新增 `i18n_build/fix_site.py`，把切换器覆盖到全部 62 页：
- 英文页「中文」= 同页切换按钮（main.js 的 data-setlang 已支持**无 href** 时调 applyLang 不跳转）
- 有语言版的页 → 直接切到对应语言页；无语言版的 22 个产品页 → 退回该语言产品列表 `/de/products.html`
- 首页统一用目录形式 `/de/`（与 canonical 一致），不用 `/de/index.html`

**踩坑**：我先用正则清掉旧切换器再重跑，结果英文页的 `lang-btn` 早已在上次被替换成 lang-switch，
清除后**没有 button 可替换**，导致 40 页切换器全空。
改 fix_switcher 为「在 `<div class="nav-actions">` 后插入；已存在则整体替换」才稳定。
→ 教训：做"清除再重建"前，先确认重建逻辑不依赖被清除的那个元素。

### 问题2：首页 Full Product Gallery 的 26 项没有链接
**原因**：26 个 `<div class="g-item">` 全是纯 div，从未包过 `<a>`。

**修复**：按产品名匹配 products-data.js 建立映射（ym1-8 / pr1-5 / fr1,2,3,5 / rb1-4 / fi1-4），
把每项改成 `<a class="g-item" href="products/<id>.html">`；四个语种首页共 104 条链接，
逐条 `os.path.exists` 校验可达。

**踩坑**：
- 语言页链接写成 `../../products/xx.html` → 从 `/de/` 出发会跳出站点根，正确是 `../products/xx.html`。
- CSS `.g-item` 原本没有 display，改成 `<a>` 后会变 inline，需补 `display:block; color:inherit; text-decoration:none`。
- 顺带发现图库第 6/7 项**图片与名称错位**（Frosted 用了 Camo 的图，反之），已按名称换回正确配图。

## 其他
- 新增 `push.sh`：带 12 次重试的 git push（代理间歇性 502/Empty reply，单次常失败）
- 长命令行 git 操作会被 OOM kill（exit 137），写成脚本文件执行更稳
- 新增 `i18n_build/fix_site.py`：图库链接 + 语言切换器修复器（幂等，可重跑）
