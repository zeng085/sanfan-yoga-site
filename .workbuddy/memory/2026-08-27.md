# 2026-08-27 工作记录

## 外贸 GEO 获客改造（对标头条文章"用 ChatGPT 做外贸"）
用户转发今日头条文章（做外贸最好出路是 ChatGPT：用 ChatGPT/Gemini/Perplexity 跑通独立站 AI 推广，声称 63 询盘/周），要求落地到 sanfan 独立站。
本质方法 = **GEO（Generative Engine Optimization）**：让 ChatGPT/Gemini/Perplexity 在海外采购商提问时主动推荐品牌，而非等传统谷歌搜索。

### 完成两件事（用户要求"同时做"）
1. **GEO 内容清单** → `GEO-Content-Plan.md`：24 个采购商真实提问（MOQ / 认证 / 定制 / 交期 / 为何选福建三梵）+ 6 个博客选题 + 5 组 AI 曝光监测关键词。
2. **改独立站**（commit `6c1945c`，Vercel 已上线 https://sanfan-yoga-site.vercel.app/faq.html）：
   - 新增 `faq.html`：24 条中英双语 FAQ（`<details>` 折叠，内容留在 DOM 可被 AI 抓）+ **FAQPage JSON-LD** 结构化数据（24 questions，线上已验证 `type=FAQPage`）。
   - 全站 5 页导航加 FAQ 入口（main.js 新增 `nav.faq` 键）。
   - 首页 Why Us 重构为"采购商最想确认的 5 件事"框架（Proven Track Record / Quality You Can Verify / Low MOQ Fast Lead Time / Full Customization）。
   - 图片 alt 增强：factory-building、certification-wall、showroom 改为"这张图证明什么"的描述性 alt。
   - sitemap.xml 加入 faq.html。

### 验证
- Playwright 线上：HTTP 200 + JSON-LD `{type:FAQPage, questions:24}` + 导航含 FAQ。
- 本地：i18n 零缺失、JSON-LD 合法、main.js 语法 OK、24/24 Q&A。

### 可复用
- 验证结构化数据需用 Playwright 读线上 `<script>` 原文（WebFetch 转 markdown 会丢 `<script>`；沙箱 curl 无外网，HTTP 000）；`faq-online.js` 模式可直接复用。
- GEO 四步法：Why Us 框架 → 采购商真实问题 FAQ → HTML 基建(H1/H2/alt/sitemap) → 每周 AI 监测曝光。
- 后续待做（未执行）：每周用 5 组监测词在 ChatGPT/Gemini/Perplexity 自检曝光。

## 博客板块落地（用户说"继续"）
在 GEO FAQ 基础上继续做内容放大——建博客板块。commit `d209e63`（Vercel 已上线）。
- 新增 `blog.html`：6 篇帖子列表卡片（双语标题/摘要，`.lang-zh`/`.lang-en` 整块切换）。
- 新增 `blog/` 下 6 篇双语文章（tpe-vs-pu / moq-explained / compliance / vet-factory / 7day-sampling / custom-sizes），每篇带 **Article JSON-LD** 结构化数据（headline/author/publisher/datePublished/mainEntityOfPage）。
- 双语机制：main.js `applyLang` 现切换 `html.show-zh`/`show-en` class；CSS 默认 `.lang-zh{display:none}`，`html.show-zh .lang-zh{display:block}` + `html.show-zh .lang-en{display:none}`（默认英文，契合 GEO 英文 AI 查询）。
- 全站 6 页（index/about/products/contact/faq/blog + 6 篇）导航加 Blog 入口（main.js 新增 `nav.blog`，CSS 加 `.nav-links a.active` 高亮）。
- sitemap.xml 加入 blog.html + 6 篇；`GEO-Content-Plan.md` 标记博客扩展已完成。
### 验证
- 本地 `validate-blog.js`：main.js 语法 OK、nav.blog 中英双键存在、6 篇 Article JSON-LD 可解析、blog.html 链接可达、双语块齐全。
- Playwright：blog EN 模式 en 显/zh 隐、切 ZH 后 zh 显/en 隐（切换正常）、post 页 Article JSON-LD 存在、无 JS 报错。
- 线上：blog.html 6 卡片 + 导航 Blog 高亮、compliance 文章 `type:Article`、sitemap 含 6 个 blog/ 条目。
### 可复用
- 双语长文用 `.lang-zh`/`.lang-en` 整块包裹 + applyLang 切 class，比逐句 data-i18n 更适合大段文章。

## 海外推广渠道调研 + 三份运营包（用户说"三个都做"）
用户问"还有没有其它能增强的推广方式"，调研后产出 3 份可执行的运营包（项目根目录）：
- `TikTok-工厂号运营包.md`：账号定位/主页模板/内容矩阵/前10条英文脚本(接青山30秒框架)/发布SOP；数据支撑 TikTok B2B 内容 +200%、工厂询盘 +180%。
- `PR-专家引用投递包.md`：HARO/Connectively+Featured+Qwoted+SOS 平台清单、专家定位、6类可答问题、英文 pitch 模板、记录表；免费拿 DA30-90 外链反哺 GEO。
- `三梵海外推广渠道地图.md`：现状盘点 + 渠道全景优先级表 + 三阶段执行路线(阶段一TikTok/PR/转介绍/WhatsApp；阶段二YouTube/目录/海关数据/Reddit；阶段三Ads/展会) + 闭环逻辑。
### 关键判断
- 用户基础盘(阿里国际站/亚马逊/LinkedIn/独立站GEO)已覆盖"搜索+信任承接"，最大增量在补"注意力+信任前置"(TikTok/PR/目录)。
- 这三类新渠道与已建 GEO 互相放大：视频/PR 让人"看见"，独立站 GEO 让人"信+被AI推"，WhatsApp/邮件转订单。
### 待办（未执行，需用户拍板）
- 把"As Seen In 媒体引用"区块加进独立站首页（PR 成果变站内信任资产）。
- 每周用 GEO-Content-Plan 的 5 组词在 3 个 AI 自检曝光。

## 修复语言切换器排版（DEJAKO 挤在一起）
用户截图反馈导航栏语言切换器显示为 "EN 中文 DEJAKO"，DE/JA/KO 因间距过小而视觉粘连。
- 修改 `assets/css/styles.css`：`.lang-switch` 的 `gap` 从 2px 提到 4px，并给每个语言项统一 `min-width: 34px`、居中、一致的 pill 样式；`<button>` 与 `<a>` 视觉统一。
- commit `a02fbd2`，Vercel 已自动部署，线上 `fjsanfan.com` 验证：EN / 中文 / DE / JA / KO 分隔清晰。
- 本次仍使用旧 GitHub PAT 推送成功；此前泄露的 DeepL key / PAT 轮换仍需用户在控制台处理。
