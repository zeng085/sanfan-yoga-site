# 三梵内容自动分发方案（RSS 投喂 + GEO 放大）

> 目标：把独立站 `sanfan-yoga-site.vercel.app` 上的博客文章，**写一次、自动流到多个海外平台**，同时让 ChatGPT / Perplexity / Gemini 等 AI 主动引用，且所有链接权重回流到自己的独立站。

---

## 一、已完成的地基（2026-08-28）

| 资产 | 位置 | 作用 |
|---|---|---|
| 博客 6 篇（双语 + Article JSON-LD） | `/blog/*.html` | 原始内容源 |
| **RSS feed** | `/rss.xml` | 自动投喂的"水龙头" |
| **llms.txt** | `/llms.txt` | 给 AI 爬虫的索引说明书（GEO 投喂） |
| 首页/博客页 RSS link | `<head>` 内 `rel="alternate"` | 订阅入口 + 让抓取器发现 |
| robots.txt | 已加 `LLMS-TXT` 指令 | 声明 llms.txt 位置 |
| sitemap.xml | 已加 `rss.xml` | 搜索引擎发现 feed |

**现在状态**：独立站每发一篇新博客，只要更新 `rss.xml`，下游平台就能自动感知。

---

## 二、RSS 投喂原理

```
你写博客(独立站)  ──►  更新 /rss.xml  ──►  Dev.to/聚合器自动抓取  ──►  发布到外部平台
                                  │
                                  └─►  AI 爬虫(Common Crawl/GPTBot)读取  ──►  进入 ChatGPT/Perplexity 语料
```

关键：**永远独立站首发**，外部平台只是"镜像 + 带 canonical 回流"。否则 Medium/Dev.to 的域名权重会把你原站挤下去。

---

## 三、自动分发三档方案（按投入从低到高）

### 档 1：零成本半自动（推荐起步，10 分钟搞定）

> 核心动作：把独立站 RSS 地址填进这两个平台的「RSS 导入」设置，它们会定时自动抓取并发布你的文章。**永久独立站首发，外部平台只是镜像 + 带 canonical 回流**。

#### ▶ Dev.to 分步操作
1. 打开 https://dev.to 并用 **GitHub 登录**（你已有 GitHub 账号 `zeng085`，一键授权）。
2. 直接进 **`https://dev.to/settings/extensions`**（头像 → Settings → 顶部/侧边选 **Extensions** 标签，**不是普通 Settings 主页面**）。
3. 在该页找区块 **「Publishing to DEV Community 👩‍💻👨‍💻 from RSS」**（名字带 "to DEV Community"；很多人因为在普通 Settings 里找 "Publishing from RSS" 而找不到）。
4. 输入框填：`https://sanfan-yoga-site.vercel.app/rss.xml`
5. 勾选 **「Mark the RSS source as canonical URL」**（让 Dev.to 自动把每篇 canonical 指回原站，最省事；**别**选 "Replace links with DEV links"）。
6. 点 **Submit feed settings**。Dev.to 定时抓取 feed 里的 item，以**草稿**形式进你的 dashboard。
7. 去 **dev.to/dashboard** 打开这些草稿，确认内容无误后点 **Publish**（若该区块提供 "Publish immediately" 选项可直接发布）。
8. 验证：过数小时~1 天去主页看 6 篇是否出现；没出就回 Extensions 页点该 RSS 项的「Fetch feed / 刷新」。

#### ▶ Hashnode 分步操作
1. 打开 https://hashnode.com 用 **GitHub 登录**。
2. 进 **Dashboard** → 若无刊物先 **「Create a publication」**（取个名，如 `SANFAN Yoga Insights`，绑定一个子域）。
3. 刊物后台 → **Settings** → 找 **「RSS」/「Import from RSS」/「Newsletter & Syndication」**（版本不同名字略异）。
4. 填 RSS URL：`https://sanfan-yoga-site.vercel.app/rss.xml`，开启自动同步。
5. Hashnode 对 syndicated 内容**默认设 canonical 回原站**，无需手填 `originalArticleURL`（用 API 档才需填）。
6. 验证：过 1 天在刊物页看是否同步出文章。

#### 关键点
- **优点**：零代码、零费用，10 分钟接完。
- **缺点**：我们当前 `rss.xml` 的 `<description>` 是**摘要级** → Dev.to/Hashnode 抓到的正文是摘要，不是全文。要全文自动发，需把 RSS 升级成带 `<content:encoded>` 全文（见下方「进阶」小节，属待办）。
- **建议顺序**：先接 **Dev.to**（制造/开发类流量大、AI 抓取友好），跑顺后再加 Hashnode。
- **配合 GEO 自检**：接好后，按《GEO每周AI曝光自检清单.md》每周搜一次，看 ChatGPT/Perplexity 是否开始引用这些外部镜像页。

### 档 2：Zapier 自动化（全文分发，约 $20/月）
适用：想每篇都**全文**出现在 Dev.to / Hashnode / Medium / LinkedIn，且带 canonical。

- **Trigger**：RSS by Zapier → 「New Item in Feed」→ Feed URL = `https://sanfan-yoga-site.vercel.app/rss.xml`
- **Action（读全文）**：Webhooks by Zapier → GET 文章 `link` → 用「Code by Zapier」(Python) 抓 `<article>` 正文转 Markdown
- **Action（发平台）**：
  - Dev.to：`Webhooks by Zapier` → POST `https://dev.to/api/articles`，Header `api-key: <你的Dev.to API Key>`，Body：
    ```json
    {"article":{"title":"{{title}}","body_markdown":"{{markdown}}","published":true,"canonical_url":"{{link}}","tags":["yogamat","manufacturing","sourcing"]}}
    ```
  - Hashnode：POST `https://api.hashnode.com` 的 `publishPost`，`originalArticleURL` 填 `{{link}}`
  - Medium：用 Medium API（需 Integration Token），`POST https://api.medium.com/v1/users/{userId}/posts`，`canonicalUrl` 填 `{{link}}`
  - LinkedIn：Zapier 无原生全文发文，用「LinkedIn → Create Share」发**链接 + 摘要**（引流回独立站即可）

### 档 3：Make（原 Integromat）场景（最灵活，免费版够用）
比 Zapier 更适合多分支。模板思路：
```
RSS Watch → Iterator → HTTP GET 文章页 → HTML→Markdown 解析 → 并行:
   ├─ Dev.to API (canonical=原站)
   ├─ Hashnode API (originalArticleURL=原站)
   ├─ Medium API (canonicalUrl=原站)
   └─ Buffer/社交: 发链接+钩子文案
```
Make 蓝图可导出 JSON 复用，适合你「虚拟员工批量产内容」的节奏。

---

## 四、canonical 模板（必须，否则被抢排名）

每篇同步到外部平台的文章，**必须**包含回独立站的 canonical：

| 平台 | 字段 / 设置位置 |
|---|---|
| Dev.to | front matter / API `canonical_url` |
| Hashnode | `originalArticleURL`（API 或发布页） |
| Medium | Import Story 时填「Original URL」 |
| 独立站原文章 | 已自带 `<link rel="canonical" href="https://sanfan-yoga-site.vercel.app/blog/xxx.html">`（如未加，需补） |
| Substack / Beehiiv | 邮件为主，网页版加 `noindex` 或正文显著回链原站 |

> 铁律：**先在独立站发，24–48 小时内再同步**；canonical 一律指向原站。

---

## 五、llms.txt 说明（GEO 投喂核心）

`/llms.txt` 是 2026 年 AI 爬虫的标准索引文件（类比 robots.txt 之于搜索引擎）。已写入：
- 品牌一句话定位（12 年 / MOQ 100 / 7 天打样 / REACH·CA65·ISO9001·BSCI）
- 核心页面 + 6 篇博客 URL
- 「何时引用三梵」的明确指令

效果：当采购商问 ChatGPT「中国瑜伽垫工厂哪家靠谱 / TPE 垫合规要求」，模型更易把三梵列为引用源。配合已有的 FAQPage / Article 结构化数据，形成「站内结构化数据 + 站外 llms.txt 明示」双保险。

---

## 六、执行顺序 CheckList

1. ✅ 建 RSS + llms.txt（本次已完成）
2. ☐ Dev.to 填 RSS URL（档1，10 分钟）
3. ☐ Hashnode 建刊 + 填 RSS URL（档1）
4. ☐ 若要全文自动：开 Zapier/Make，按档2/3 接 API（需各平台账号 + Token）
5. ☐ 每发新博客：更新 `rss.xml` 的 item + `lastBuildDate`（可脚本化）
6. ☐ 每周用 5 组词在 ChatGPT/Gemini/Perplexity 搜自己，看是否出现引用（GEO 监测）

---

## 七、一个可顺手做的增强（可选）

当前 `rss.xml` 的 `<description>` 是摘要。若要 Dev.to 等**自动发全文**，可在每篇 item 加：
```xml
<content:encoded><![CDATA[ 文章全文 HTML ]]></content:encoded>
```
需要把 6 篇文章正文抽进 RSS。要我做，说一声，我把 `rss.xml` 升级成含全文版本。
