# 三梵独立站 · 收录前提体检报告

- 站点：https://fjsanfan.com
- 检查时间：2026-08-30
- 检查范围：sitemap.xml 全部 69 个页面（含 EN/ZH/DE/JA/KO 五语种）
- 抓取身份：Googlebot UA，验证「Google 来抓时会看到什么」

---

## 一、先说结论

| 项目 | 结果 |
|---|---|
| 页面可访问（HTTP 200） | **69 / 69 全部通过** |
| 误设 noindex | **0** |
| canonical 自引用错误 | **0** |
| H1 缺失或重复 | **0** |
| 缺 title | **0** |
| title 超长（>60 字符） | 12 → **修复后仅剩 4 条（均为英文，不影响展示）** |
| hreflang 不完整 | 7 条（全部为 blog 页，**设计如此，非缺陷**） |
| 内容过薄 | 11 条告警 → **复核后全是误报** |

**技术层面没有阻断收录的问题。** 剩下的是时间问题 + 需要你去 Google Search Console 主动提交。

---

## 二、为什么查不到真实收录数

| 渠道 | 结果 | 原因 |
|---|---|---|
| Google `site:fjsanfan.com` | ❌ 查不了 | 被反爬拦截（"unusual traffic"），服务器出口是机房 IP `35.226.235.125`，Google 直接拒绝 |
| Bing `site:fjsanfan.com` | ❌ 结果不可信 | 代理环境下返回的是 YouTube / Reddit 等完全无关的噪音结果 |
| 通过 ChatGPT 等第三方 | ❌ 拿不到正文 | 分享页有登录墙 |

**真实收录数只有两条路能拿到：**

1. **你自己在本机浏览器**搜 `site:fjsanfan.com`（住宅 IP，不会被拦）—— 最快，30 秒
2. **Google Search Console**（见第四节）

---

## 三、已修复的问题

### 德语 title 严重超长（commit `d34f7762`，已上线）

德语单词比英语长，英文站的后缀 `— SANFAN Yoga & Fitness Manufacturer` 译成德语后膨胀成
`– SANFAN Hersteller von Yoga- und Fitnessartikeln`，导致 title 涨到 **63–95 字符**。
Google 搜索结果约 60 字符就截断，**品牌名 SANFAN 直接被砍掉**。

修法：产品页 title 对齐英文站格式 `产品名 — SANFAN Yoga & Fitness Hersteller`。

| 页面 | 修复前 | 修复后 |
|---|---|---|
| `/de/faq.html` | 95 | 51 |
| `/de/products/ym3.html` | 82 | 38 |
| `/de/products/fr1.html` | 80 | 39 |
| `/de/products/ym2.html` | 74 | 55 |
| `/de/products/rb2.html` | 72 | 57 |
| `/de/` | 70 | 46 |
| `/de/products/ym1.html` | 63 | 48 |
| `/de/contact.html` | 65 | 35 |

顺带修掉两处瑕疵：
- `ym3` 标题的 `TPE -Matte` 多余空格 → `TPE-Matte`
- `ym2` 的德语引号 `„TPE“` 噪音去掉
- `/de/faq.html` 的 `og:title` 也从 111 字符降到 52

### 保留未改的 4 条英文 title（62–66 字符）

| 页面 | 字符 | 为什么不动 |
|---|---|---|
| `/products/fr1.html` | 66 | 砍掉 "Yoga & Fitness" 会损失核心品类词 |
| `/blog/compliance.html` | 64 | `REACH / CA Prop 65` 是采购商真实搜索词，价值高于长度 |
| `/products/pr3.html` | 62 | 同上 |
| `/products/fi5.html` | 62 | 同上 |

**判断依据**：Google 按**像素**（桌面约 600px）截断而非字符数。62 字符的英文 title 约 430px，
**实际不会截断**。为了凑数字砍掉高价值关键词不划算。

---

## 四、⚠️ 关于「内容过薄」的误报（重要，避免以后重复踩坑）

体检脚本最初报了 11 条「内容过薄」，全部是**误报**。

原因：脚本用 `len(text.split())` 按空格分词统计词数，但**日语/韩语/中文没有空格**。

实测复核：

| 页面 | 空格分词"词数" | 实际字符数 |
|---|---|---|
| `/faq.html`（英文） | 1086 | 6719 |
| `/ja/faq.html`（日文） | **134** | **4222** |
| `/products/ym1.html`（英文） | 500 | 3233 |
| `/ja/products/ym1.html`（日文） | **145** | **1674** |

日文 4222 字符对应约千词的内容量，完全正常。
**结论：评估 CJK 语系内容量必须用字符数，不能用词数。**

---

## 五、你需要做的：Google Search Console 提交（约 10 分钟）

Google 不支持 IndexNow（Bing / Yandex / Seznam 已推送过），只能走 GSC。

1. 打开 https://search.google.com/search-console
2. 选「网址前缀」→ 填 `https://fjsanfan.com/`
3. 验证所有权：Cloudflare 后台加一条 **TXT 记录**（GSC 会给你一串值）
4. 左侧「站点地图」→ 提交 `sitemap.xml`
5. 顶部搜索框用「网址检查」手动请求索引这几个关键页：
   - `/`（首页）
   - `/products.html`
   - `/faq.html`
   - `/products/ym1.html`
   - `/blog/tpe-vs-pu.html`

提交后 Google 通常 **3–14 天**开始收录，GSC 的「网页」报告能看到准确数字——
那才是唯一可信的收录数据，`site:` 命令的估算数字从来不准。

---

## 六、当前状态

- GitHub 远程 HEAD：`d34f7762`
- 本地 HEAD：`a02fbd2`（落后一个 API 提交的 commit）
- 对齐方式（网络恢复后）：
  ```bash
  git fetch origin && git reset --hard origin/main
  ```
  工作区内容不会变，因为远程内容与本地一致。

> 注：本次推送因 `github.com` 被代理阻断（502），改用 GitHub REST API 完成提交，
> 所以本地与远程 commit SHA 不同。内容完全一致，不影响部署。
